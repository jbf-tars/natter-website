---
import ModeCard from "../ui/ModeCard.astro";
---

<section class="px-6 py-24">
  <div class="mx-auto max-w-5xl">
    <h2 class="reveal text-center text-3xl font-bold md:text-4xl">
      Three modes for how you think
    </h2>
    <p class="reveal mx-auto mt-4 max-w-xl text-center text-text-muted">
      Whether you're writing an email, dumping thoughts, or prompting an AI — Natter adapts to your workflow.
    </p>

    <div class="mt-16 grid gap-6 md:grid-cols-3">
      <div class="reveal reveal-delay-1">
        <ModeCard
          title="Normal"
          badge="Free Trial"
          badgeColor="bg-emerald-600"
          description="Clean, faithful dictation. Say what you mean, get polished text with filler words and false starts removed."
          before="So um I wanted to say that the meeting is uh moved to Thursday at like 3pm"
          after="The meeting is moved to Thursday at 3pm."
        />
      </div>

      <div class="reveal reveal-delay-2">
        <ModeCard
          title="Ramble"
          badge="Pro"
          badgeColor="bg-primary"
          description="ADHD-friendly brain dumps. Speak freely and messily — Natter restructures your scattered thoughts into clear, organized text."
          before="ok so basically I have this idea where we could like do a thing where the app sends notifications but not annoying ones more like helpful reminders and maybe we could tie it into the calendar somehow I dunno"
          after="Feature idea: Smart notification system that sends helpful (non-intrusive) reminders, potentially integrated with the user's calendar."
        />
      </div>

      <div class="reveal reveal-delay-3">
        <ModeCard
          title="Agentic Engineer"
          badge="Pro"
          badgeColor="bg-primary"
          description="Voice-to-engineering prompts. Describe what you want to build, and Natter crafts a structured prompt ready for AI coding tools."
          before="I need a react component that shows a list of users with their avatars and you can click to expand their profile details and it should use that accordion pattern"
          after="Create a React component `UserList` that renders an array of users. Each item displays: avatar (circular, 40px), full name, and role. Clicking a row expands an accordion panel showing: email, join date, and bio. Use Radix UI Accordion primitive. Props: `users: User[]`. Include TypeScript types."
        />
      </div>
    </div>
  </div>
</section>
